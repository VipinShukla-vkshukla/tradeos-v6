# TradeOS v6 — Python Dependencies

# Core
supabase==2.10.0
python-dotenv==1.0.1
loguru==0.7.2
pandas==2.2.2
numpy==1.26.4
requests==2.32.3
pytz==2024.1

# Google Sheets API
google-api-python-client==2.137.0
google-auth==2.32.0
google-auth-httplib2==0.2.0

# Phase 1 — AI providers (install as needed)
anthropic==0.34.2
openai==1.45.0
google-generativeai==0.8.1
# deepseek uses openai-compatible API — no separate package needed
# grok uses openai-compatible API — no separate package needed

# Phase 1 — ML fallback
scikit-learn==1.5.1
joblib==1.4.2

# Phase 1 — Web scraping fallback
beautifulsoup4==4.12.3
lxml==5.2.2

# Phase 1 — Telegram
python-telegram-bot==21.5

# Phase 2 — Kite
kiteconnect==5.0.1

# Utilities
schedule==1.2.2
python-dateutil==2.9.0
openpyxl==3.1.5
